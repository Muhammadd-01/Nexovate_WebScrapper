# Google API Key is OPTIONAL – only enhances PageSpeed scores if provided.
# Business search uses free OpenStreetMap/Overpass API (no key needed).
GOOGLE_API_KEY=

# MongoDB Atlas connection string (required for data storage)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
DATABASE_NAME=lead_intelligence
